<%= render("_poll.html", %{poll: @poll, conn: @conn}) %>

<i id="enable-polls-channel" data-poll-id="<%= @poll.id %>"></i>
<button id="polls-ping" class="btn btn-primary">Ping Websocket</button>

<%= render(VocialWeb.SharedView, "_chat.html") %>